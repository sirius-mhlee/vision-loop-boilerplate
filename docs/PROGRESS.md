# 구현 진행 상태

## 이번 구현

기준 계획: [PLAN.md](PLAN.md). 원본 계획 본문은 보관하고 현재 안내로 연결하는 주석을 붙였습니다.
아래 단계별 검증은 당시의 기록이며, 최신 사용법·정책은 [README](../README.md)를 따릅니다.
참고 형식: [DACON-236092-SemanticSegmentation](https://github.com/sirius-mhlee/DACON/tree/main/DACON-236092-SemanticSegmentation).

기존 코드의 `argparse`, `dataclass`, 평탄한 YAML, 기능별 모듈, 짧은 실행 절차를 따릅니다.
패키지·CLI 이름은 계획의 `vloop`를 사용하므로 구현은 `src/vloop`에 둡니다.
설정에서 알 수 없는 키를 조용히 버리던 동작은 실험 설정 오타를 발견하도록 오류로 바꿨습니다.

| 단계 | 상태 | 확인 범위 |
|---|---|---|
| 1. 환경·설정·CLI | SAM 3 추론 확인 | Python 3.12, 설정 검증, 실행 기록, CUDA 연산과 실제 SAM 3 추론 |
| 2. 이미지 등록·자동 라벨링 | 샘플 통합 검증 완료 | 중복 방지, 원본 보존, 작업별 예측, 고정 설정으로 다른 프로세스에서 재개 |
| 3. FiftyOne 검수 | 대량 채택 경로 추가 | 수동 승인·자동 채택 구분, 중단·재개, 표본·불확실 예측 검수, 증분 감사 |
| 4. COCO·DVC 릴리스 | 구현·통합 검증 | 승인 검증, 스트리밍 COCO RLE, 고정 split, DVC 저장·Git 태그·격리 복원 |
| 5. 학습·MLflow | 구현·GPU 통합 검증 | 실제 짧은 학습, MLflow 기록·화면, 별도 프로세스 체크포인트 재개 |
| 6. 모델 복원·평가 | 구현·통합 검증 | 학습 작업 ID 기반 평가, 박스/마스크 COCO 지표, MLflow 아티팩트·FiftyOne 분석 복원 |
| 7. 반복 개선 | 공개 toy 두 차례 실행 | SAM 3 → 공개 정답 승인 → v001/v002 학습 → 동일 val 비교·과거 버전 복원 |

## 환경 관찰 (2026-09-06)

- Python 3.12.3 가상환경 `.venv`를 생성했습니다.
- 호스트 `nvidia-smi`: NVIDIA GeForce RTX 5060 Laptop GPU, 드라이버 580.173.02, 약 8 GB VRAM.
- PyTorch 2.10.0+cu128, torchvision 0.25.0+cu128 설치 후 CUDA 행렬 연산 결과를 CPU 기대값과
  비교해 통과했습니다. 장치 compute capability는 12.0입니다.
- 격리 환경의 `nvidia-smi` 실패와 호스트 GPU 정상 상태를 구분했습니다. 드라이버는 변경하지 않았습니다.
- FiftyOne 1.21.0과 SAM 3 0.1.0을 설치했습니다. SAM 3는 사용자가 준비한 로컬 소스를
  editable로 설치했으며 커밋은 `660a5e9e1b8b4c02c0ad97229b88a09a6e4ff5b7`입니다.
- 사용자가 내려받은 `.vloop/models/sam3/sam3.pt`를 실제 `project.yaml`에 연결했습니다.
  체크포인트 SHA-256은 `9999e2341ceef5e136daa386eecb55cb414446a00ac2b55eb2dfd2f7c3cf8c9e`입니다.
- NumPy 1.26.4, OpenCV headless 4.11.0.86, SciPy 1.16.3, tifffile 2025.5.10,
  einops 0.8.2, pycocotools 2.0.11 조합을 설치하고 `autolabel` extra에 기록했습니다.
- 당시 RF-DETR·MLflow·DVC는 설치 전이었습니다. 이후 설치·검증은 아래 4·5단계에 기록합니다.
  전체 의존성 lock은 이후 확정했으며 아래 의존성 버전 고정 절에 기록합니다.
- 실제 데이터 폴더·클래스·프롬프트는 미입력 상태입니다. 검증에는 별도 설정과 SAM 3 공식
  샘플을 사용했으며, 실제 프로젝트에 예시 클래스를 넣지 않았습니다.

## 자동 라벨링 구현

- `vloop autolabel [--limit N]`, `vloop autolabel --resume JOB_ID`를 추가했습니다.
- 작업별 SQLite 입력·상태 목록, 최종 설정, SAM 3 커밋·체크포인트·어휘·adapter·의존성 해시를
  보관합니다. 재개에는 원래 설정을 사용하며 완료 결과를 다시 추론하지 않습니다.
- 예측 JSON을 먼저 저장한 뒤 FiftyOne에 반영합니다. DB 쓰기만 실패한 이미지는 추론 결과를
  재사용합니다. 처리 상태와 정상 빈 예측을 구분하고 실패한 이미지의 원인을 남깁니다.
- 예측 필드는 작업별로 만들며 `ground_truth`와 검수 상태를 수정하지 않습니다.
- 공통 표현에 클래스 ID, 프롬프트, 신뢰도, 픽셀 박스, 전체 이미지 크기의 COCO RLE를
  저장합니다. FiftyOne의 박스 내부 마스크와 변환할 때 크기·좌표·면적을 검사합니다.
- 8 GB 환경을 위해 이미지·프롬프트를 하나씩 처리하고 BF16을 사용합니다. 모델은 작업 중
  재사용하며 컴파일은 끕니다. FiftyOne 1.21의 내부 점수 필터도 사용자 임계값에 맞춥니다.

## FiftyOne 검수 구현

- `vloop review [--job-id JOB_ID] [--prepare-only] [--no-browser]`를 추가했습니다.
  기본값은 가장 최근 등록된 자동 라벨링 작업입니다. 실행 기록에 선택한 작업과 초기화·보존·
  예측 없음·승인 무효화 건수를 남깁니다.
- 검증한 예측 JSON에서 새 라벨 ID로 `ground_truth`를 초기화합니다. 기존 수동 정답과 검수
  이력을 보존하고, 검수용 정답을 나중에 삭제해도 자동 예측으로 덮어쓰지 않습니다.
- 클래스 선택·마스크 수정을 위한 Annotation Schema, 읽기 전용 예측 필드, 미검수·수정 중·
  완료·제외 저장 뷰를 만듭니다. Python 검수 operator는 프로젝트 저장소 안에 설치합니다.
- 열린 이미지 또는 직접 선택한 이미지에 검수 시작/재검수·완료·제외를 적용합니다.
  완료 시 확인 체크와 검수자 이름이 필요하며, 빈 정답은 객체가 없다는 별도 확인을 요구합니다.
- 승인 시 정답 스냅샷·해시·출처·검수자·시간을 JSON과 DB 이력에 남깁니다. 승인과 동시에
  마스크가 수정되면 조건부 DB 갱신이 실패하도록 하여 편집 내용을 덮어쓰지 않습니다.
- 네이티브 브러시가 화면 해상도의 마스크와 소수 박스를 저장하는 것을 확인했습니다.
  승인 시 원본 픽셀 경계를 반올림하고 nearest-neighbor로 전체 이미지 RLE를 만듭니다.
  편집 원본의 박스·마스크 RLE도 별도로 보존합니다.
- 서버는 인덱스와 저장한 조회 위치로 DB 수정 내역을 따라갑니다. 기본 5초마다 최대
  10,000건의 메타데이터를 스트리밍으로 읽고 변경된 승인 최대 100건만 검사합니다.
  외부 이미지·기록 파일을 포함하는 전체 검사는 `review-audit`로 따로 실행합니다.
  후속 릴리스용 `approved_annotation()`은 기본적으로 수동 승인만 허용합니다.
- FiftyOne 1.21의 펼친 이미지 상태 표시는 operator의 reload 요청 후에도 이전 값이 남는
  경우가 있습니다. DB 변경은 성공하며 브라우저 새로고침 후 최신 상태가 보입니다.
  실제 대규모 DB·이미지 처리 성능은 후속 확인 대상입니다.

## 100만 장 이상을 위한 검수 보완

- `review-batch`로 작업 ID·최소 신뢰도·검수 표본 비율·정책 선택자를 지정합니다.
  먼저 채택·표본·낮은 신뢰도·빈 예측·보존·오류 건수를 미리 계산하고, 그 작업 ID에
  `--resume ID --apply`를 실행해 적용합니다. 구현 검증 중 실제 사용자 데이터에 적용하지 않았습니다.
- 일괄 채택은 `auto_accepted`/`automatic`, 수동 승인은 `completed`/`manual`로 구분합니다.
  자동 채택을 후속 릴리스에 포함하려면 `allow_auto=True`를 명시해야 합니다.
- 낮은 신뢰도·빈 예측·무작위 표본을 각각 저장 뷰로 제공합니다. 당시에는 고신뢰도에서 표본을
  뽑았으며, 후속 구현에서 미검수 후보 전체를 대상으로 변경했습니다. 이미지 ID·seed를 사용해
  배치 크기와 재개 시점이 달라도 표본을 유지합니다.
- 대상 ID·예측 해시·수정 시점을 SQLite 스냅샷으로 고정하고, 기준이나 스냅샷이 변경되면
  재개를 거부합니다. 메모리에는 기본 100개 ID와 처리 중인 이미지의 라벨만 유지합니다.
- 중단한 미리보기·적용을 이어갈 수 있습니다. DB 저장 성공 후 진행 기록이 남기 전에
  중단된 경우도 복구하며 같은 배치로 재승인하지 않습니다. 수동 수정이 있으면 보존합니다.
- 자동 채택 스냅샷은 `reviews/records.sqlite3`에 모아 새 JSON 파일 100만 개를 만들지 않습니다.
  수동 승인·재검수 이력과 원본 자동 예측은 보존합니다.
- 화면 시작 시 정답 초기화는 기본 100장으로 제한합니다. 검수 목록의 나머지 마스크는
  필요할 때 초기화합니다. 수동 operator도 최대 100장으로 제한하고 대량 작업은 CLI로 처리합니다.
- 전체 무결성 검사에는 `review-audit [--resume ID]`를 사용합니다. 증분 검사는
  수정 시점을 갱신하지 않는 직접 DB 조작·외부 파일 변경을 대신 확인하지 않습니다.
- 이는 검수 단계의 메모리·반복 조회·파일 수를 보완한 것입니다. 기존 ingest/autolabel의
  전체 해시 검사, 이미지별 JSON·PNG 저장, 실제 모델 처리량과 MongoDB 디스크 사용량까지
  100만 장의 실제 데이터로 검증한 것은 아닙니다.

## 1~3단계 검증 결과 (2026-09-06)

- 기본 테스트 97개 통과. 실제 DB·GPU 테스트 4개는 기본 실행에서 명시적으로 제외합니다.
- 별도 호스트 실행에서 FiftyOne 통합 테스트 3개 통과: 실제 등록, 다른 프로세스의 재등록,
  중복 방지, `ground_truth`와 검수 상태 보존, 예측 필드 생성 도중 중단된 상태의 복구를 확인했습니다.
- 검수 통합 테스트는 정답 복사·삭제 후 보존, 승인·마스크 변경 감지·재승인·제외,
  빈 정답과 예측 없음의 구분, 클래스 수정 후 ID 매핑, 동시 수정 충돌, 플러그인 등록을 확인합니다.
  기본 테스트는 마스크 좌표 변환과 승인 기록·이미지 변경 및 상태 문자열만의 승인 우회를 검사합니다.
- 일괄 채택 테스트는 미리보기·적용 중단, 새 프로세스에서 DB 저장 직후 중단 복구,
  수정 내용 보존, 표본 목록과 제한된 초기화, 자동 채택의 명시적 허용, 변경 없는 승인의
  해시 검사 생략, 수정 후 무효화, 전체 이미지 검사와 정책 변경 거부를 확인했습니다.
- 100만 건 합성 메타데이터 → SQLite 스냅샷 측정: 약 3.98초, 최대 RSS 44.73 MiB,
  작업 중 RSS 증가 2.53 MiB, SQLite 약 297.07 MiB. 실제 100만 이미지나 MongoDB 처리량의
  측정값이 아닙니다. 검증 기록은 `.vloop/review-scale.json`에 있습니다.
- 실제 브라우저에서 공식 트럭 샘플의 마스크를 Brush로 수정하고 `ui-integration-test` 이름으로
  검수 완료를 실행했습니다. 화면 해상도의 마스크도 재승인됐고, 다시 수정하면 승인 해시가
  제거되고 `Ground truth changed after approval` 사유로 수정 중 상태가 됐습니다.
  자동 예측 JSON의 해시는 유지됐습니다. 이는 별도 데모의 기능 검증이며 실제 데이터 승인이 아닙니다.
- 실제 GPU 통합 테스트 1개 통과: 공식 `truck.jpg`, `groceries.jpg`를 등록하고 클래스 ID
  `7 → truck`, `42 → apple`로 처리했습니다. 첫 장 후 중단하고 현재 YAML의 클래스를 변경한
  뒤 새 프로세스로 재개해도 원래 설정으로 나머지 한 장만 처리했습니다. 첫 결과의 해시와
  사람이 수정한 정답·검수 상태, DB 저장 후 마스크 면적이 유지됐습니다.
- 비정방형 이미지와 구멍·분리 영역 마스크의 RLE 변환, 실패 이미지 재개, DB 실패 후
  결과 재사용, 설정·입력·결과 변경 감지, 스냅샷 생성 중 중단의 복구 안내를 테스트했습니다.
- `ruff check`, `ruff format --check`, `pip check` 통과. wheel 빌드에 플러그인 manifest가
  포함되는 것을 확인하고 로컬 editable 설치를 갱신했습니다.
- 샘플 설정으로 실행한 `doctor --sam3-image`는 20개 통과, 3개 실패입니다.
  실패는 RF-DETR·MLflow·DVC 미설치이며 `sam3_inference`는 통과했습니다.
  실제 `project.yaml`은 데이터·클래스가 비어 있어 필수 입력 검사도 아직 통과하지 못합니다.

### 실제 GPU 측정

| 항목 | 결과 |
|---|---|
| 입력 | 공식 `truck.jpg`, 1800×1200, 프롬프트 `truck` |
| 결과 | 트럭 1개, 신뢰도 0.8671875, 박스 xywh `[84, 282, 1625, 566]` |
| 정밀도·배치 | BF16, 이미지 1장·프롬프트 1개 |
| PyTorch 최대 할당 / 예약 메모리 | 5.2624 / 5.5098 GiB |
| 모델 로딩 포함 실행 시간 | 약 8.77초 |
| 별도 프로세스의 재개 | 완료 2장, 이번 시도 처리 1장, 최대 할당 5.2629 GiB |

이는 샘플에 대한 관찰값입니다. 전체 GPU 프로세스 메모리나 임의의 해상도·객체 수에 대한
보장값이 아니며, 실제 데이터도 작은 묶음으로 먼저 확인해야 합니다.

로컬 결과:

- 한 장 추론: `.vloop/smoke/state/runs/doctor_20260906T121142_f8085e8b/`
- 시각화: 위 폴더의 `sam3-preview.png`
- 중단·재개 통합 검증 기록: `.vloop/smoke/sam3-integration.json`
- 검수 데모: `.vloop/review-demo/project.yaml`
- 브라우저 검증 기록: `.vloop/review-demo/ui-verification.json`
- 데모 자동 라벨링: `.vloop/review-demo/state/runs/autolabel_20260906T131026_da3275e9/`
- 데모 승인·재검수 이력: `.vloop/review-demo/state/reviews/`
- 100만 건 합성 메타데이터 스냅샷: `.vloop/review-scale.json`

현재 로컬에서 검수 데모를 다시 여는 명령:

```shell
.venv/bin/vloop review --config .vloop/review-demo/project.yaml
```

데모 파일은 Git에서 제외되며, 같은 명령을 새 clone에서 바로 실행할 수는 없습니다.

## 4단계 검증 (2026-09-07)

- `release --version`, `release --prepare-only`, `release --resume`, `restore --version`을 추가했습니다.
- 수동 승인을 기본으로 하고 자동 채택은 `--include-auto-accepted`에서 train에만 허용합니다.
  최초 그룹 해시 분할·기존 split/COCO ID 보존·평가 그룹에 새로 들어온 이미지 보류를 검증했습니다.
- SQLite에 승인 스냅샷·분할 이력·라벨 출처를 고정하고 COCO JSON을 스트리밍으로 생성한 뒤
  다시 읽어 검증합니다. 자동 승인 SQLite 연결도 수집 중 공유해 이미지마다 DB를 재연결하지 않습니다.
- DVC 3.67.1의 실제 로컬 remote 저장, remote 내용 해시 검증, 로컬 캐시 삭제 후 복원,
  재복원 시 파일 재사용, 업로드 후 중단과 별도 프로세스 재개를 확인했습니다.
- Git 태그에 이미지/DB가 들어가지 않으며 현재 HEAD·스테이징·미커밋 코드가 보존되는 것을
  테스트했습니다. 태그를 직접 체크아웃하는 경우에도 데이터가 Git에 노출되지 않도록 ignore를 포함합니다.
- RF-DETR 1.8.2 `CocoDetection(include_masks=True, remap_category_ids=True)`으로 내보낸
  실제 파일을 읽어 원본 크기, 희소 클래스 ID 매핑, 빈 라벨, 구멍·분리 영역의 픽셀 일치를 확인했습니다.
- 실제 FiftyOne DB에서 3장 승인 → 릴리스 → 복원, 승인 후 수정한 라벨의 릴리스 거부를
  확인했습니다. 기존 검수/일괄 채택 통합 테스트도 함께 통과했습니다.
- 전체 통합 테스트: **109 passed, 1 skipped**. 이번 단계에 관련 없는 SAM 3 GPU 추론 검사는
  다시 실행하지 않았습니다. 의존성 `pip check`, Ruff, Git 공백 검사도 통과했습니다.

합성 메타데이터 규모 검증 (`tests/release_scale_runner.py`):

| 항목 | 10만 건 | 100만 건 |
|---|---:|---:|
| COCO 생성 | 4.05초 | 39.26초 |
| 내보낸 COCO 재검증 | 3.05초 | 29.98초 |
| 프로세스 최대 RSS | 73.53 MiB | 196.69 MiB |
| 입력 SQLite | 60.14 MiB | 601.33 MiB |
| COCO JSON 합계 | 28.89 MiB | 289.81 MiB |

객체가 없는 합성 메타데이터의 관찰값입니다. 실제 이미지·마스크의 크기, 검수 이력,
MongoDB 읽기, DVC 전송, RF-DETR 학습의 성능을 측정한 결과는 아닙니다. 결과는
`.vloop/release-scale.json`, `.vloop/release-scale-100k.json`에 있고 임시 대형 파일은 정리했습니다.
실제 대형 데이터에서는 라벨/이력 SQLite와 변경된 COCO JSON이 버전마다 차지하는 공간도
측정해야 합니다. 상세 저장 구조와 재개 절차는 [README의 Release / DVC](../README.md#release--dvc)에 있습니다.

## 5단계 검증 (2026-09-10)

- `train --dataset-version`, `train --resume <TRAIN_JOB_ID>`, `experiments [--no-browser]`를 추가했습니다.
  RF-DETR 1.8.2의 공식 Lightning 구성요소를 사용하고 Lightning 2.6.5를 고정했습니다.
  MLflow 3.16.0을 설치하고 공식 Seg Nano 가중치를 내려받아 검증했습니다.
- 학습은 DVC 릴리스를 자동 복원하고, 확정된 train/val과 릴리스의 클래스 매핑을 사용합니다.
  승인된 빈 정답도 읽으며, train/val 전체에 정답 객체가 없는 경우는 시작을 거부합니다.
  최종 test는 자동 실행하지 않습니다. 학습 코드에는 커밋된 상태를 요구합니다.
- MLflow SQLite에 실행 상태·메모·에포크별 손실·검증 지표를 기록합니다. 아티팩트에는 전체
  모델·학습 기본값, 프로젝트 설정, 데이터 태그 커밋·DVC 정보, 학습 코드 커밋, 의존성,
  클래스 매핑, 전후처리와 초기 가중치 해시를 저장합니다.
- regular/EMA 중 validation mask mAP가 가장 높은 추론용 모델을 저장합니다. 재개용 파일에는
  옵티마이저·스케줄러·에포크·step·EMA·난수 상태와 이전 최적 모델을 함께 보관합니다.
  에포크 파일을 완성한 뒤 재개 참조를 원자적으로 갱신하고 이전 파일을 정리합니다.
- 재개는 원래 run을 참조하는 새 run이며, YAML을 바꿔도 원래 데이터와 학습 설정을 사용합니다.
  코드·의존성 불일치, 설정·태그·체크포인트 변경, 이미 완료한 학습의 재개를 거부합니다.
  중단은 KILLED, 오류는 FAILED, 완료는 FINISHED로 기록하며 강제 종료 시의 한계도 문서화했습니다.
- 설치된 RF-DETR/Lightning에서 반환 손실을 각각 누적 횟수로 나누는 동작을 확인했습니다.
  adapter에서 한 번만 평균내도록 보정하고 실제 Lightning의 작은 수치 테스트로
  batch 4와 batch 1 × 누적 4의 파라미터 갱신이 일치하는 것을 확인했습니다.
- N개 프로젝트 클래스 외 추가 출력 채널을 top-k 전에 제외해 검증과 복원 추론에 적용합니다.
  희소 클래스 ID `7`, `42`와 학습에 객체가 없는 클래스도 저장 매핑을 유지했습니다.
  `trained_model.load_model()`은 새 프로세스에서 vloop 작업 ID로 최적 가중치와 구성을 복원합니다.
  후속 6단계에서 이 복원 인터페이스에 평가 CLI와 FiftyOne 평가 데이터 구성을 연결했습니다.
- 사용자가 다루는 학습 ID를 vloop 작업 ID로 통일했습니다. 로컬 보고서의 내부 MLflow ID를
  조회하고 MLflow 태그와 대조해 재개·모델 복원에 사용합니다. 재개는 새 vloop 작업과
  새 MLflow run을 만들며 원래 작업 ID를 출처로 기록합니다. MLflow 초기화 실패도 로컬에
  기록하지만, 체크포인트가 생기기 전 실패에는 새 학습이 필요합니다. 원본 PLAN의
  `--resume-run-id` 표기는 보관하고 현재 명령은 사용자와 논의한 `--resume`로 변경했습니다.
  ID 정리 후 기본 테스트 **112 passed, 8 skipped**, 별도 RF-DETR GPU 통합 테스트 **1 passed**를
  확인했습니다. 초기화 실패의 로컬 기록, 다른 작업을 가리키는 매핑의 거부, 작업 ID로 별도
  프로세스에서 학습 재개·모델 복원을 검증했고 Ruff와 Git 공백 검사도 통과했습니다.
- 실제 MLflow 서버의 localhost 화면·run 조회·아티팩트 목록을 HTTP로 확인했습니다.
  CLI 중단 시 서버 프로세스를 정리하며 SQLite에 남은 실행 기록을 다시 조회했습니다.
- 기존 FiftyOne·DVC와 새 MLflow 통합 테스트를 함께 실행해 **116 passed, 2 skipped**를
  확인했습니다. 두 제외 항목은 별도 실행하는 SAM 3/RF-DETR GPU 검사이며, RF-DETR은 아래
  실제 GPU runner로 따로 검증했습니다. 이전 최적 모델 보존·체크포인트 저장 중단 시 이전
  참조 보존 테스트와 Ruff·의존성·Git 공백 검사도 통과했습니다.

격리된 임시 Git 저장소와 생성 이미지 4장으로 실제 DVC 릴리스 → GPU 학습 → 첫 에포크 후
중단 → 별도 프로세스 재개 → 또 다른 프로세스에서 모델 복원·추론을 확인했습니다. 사용자
프로젝트의 Git 커밋이나 실제 데이터 릴리스는 생성하지 않았으며 테스트 DB·모델은 정리했습니다.

| 항목 | 결과 |
|---|---|
| 입력 | 23×13 생성 이미지 4장; train 2장(빈 정답 포함), val 1장, test 1장 |
| 모델·설정 | RF-DETR Seg Nano, 기본 해상도 312·다중 해상도 증강, batch 1, 누적 8, BF16 |
| 중단·재개 | 총 2 epochs 중 첫 epoch 후 중단; optimizer global step 5 → 10 |
| 설정 보존 | 현재 YAML을 epochs 99·학습률 0.5로 바꿔도 원래 2 epochs·학습률 0.0001로 재개 |
| 최대 PyTorch 할당 / 예약 VRAM | 재개 실행 1.253 / 1.297 GiB |
| 최대 프로세스 RSS | 재개 실행 4,199.93 MiB (약 4.10 GiB) |
| 복원 추론 | 원본 13×23 크기 마스크, 저장된 두 클래스 인덱스만 출력 |

위 값은 소형 생성 데이터에서의 동작·메모리 관찰이며 모델 정확도나 실제 대형 데이터의
메모리 상한을 뜻하지 않습니다. 기록은 `.vloop/train-integration.json`에 있습니다.

별도 프로세스로 **이미지 100만 건·이미지당 단순 RLE 객체 1개**의 합성 COCO를 실제 RF-DETR
로더에 넣었습니다. JSON 275.29 MiB, 인덱스 로딩 약 6.19초, 프로세스 최대 RSS 2,323.74 MiB
(약 2.27 GiB)였습니다. 실제 픽셀 읽기·학습·검증 예측 누적은 포함하지 않았습니다.
`train_num_workers: 0`을 기본으로 두어 인덱스의 작업 프로세스 복제 비용을 줄였으며, 복잡한
마스크·많은 객체와 큰 val의 메모리는 후속 측정 대상입니다. 기록은
`.vloop/train-loader-scale.json`이고 임시 대형 COCO 파일은 정리했습니다.

## 평가 구현 (2026-09-11)

- `vloop evaluate --job-id TRAIN_JOB_ID`로 기본 val 평가, `--split test`로 명시적 최종 평가를
  실행합니다. 데이터 버전은 기본적으로 학습 버전을 사용하고 `--dataset-version`으로
  다른 확정 버전을 지정할 수 있습니다. 원본 PLAN의 `--run-id` 대신 사용자와 합의한
  vloop 작업 ID를 사용합니다. 평가는 독립적인 작업 폴더·MLflow run을 남깁니다.
- 릴리스 태그·내용과 모델 클래스 매핑을 검사하고, 저장한 최적 모델의 전후처리 설정으로
  원본 크기의 박스와 마스크를 추론합니다. 현재 YAML의 클래스·임계값 변경은 기존 모델에
  자동 반영되지 않습니다. 의도한 변경은 평가 명령의 임계값·최대 검출 수 옵션으로 기록합니다.
- 선택 입력·고정 정답·예측은 하나의 SQLite 파일에 저장합니다. 원본 이미지 바이트는
  중복 아티팩트로 저장하지 않습니다. `--limit`은 이미지 ID 순서로 처음 N장만 선택합니다.
- 검수 DB와 분리한 평가 데이터셋에 읽기 전용 GT·예측과 실행별 평가 키를 만듭니다.
  모델 박스와 마스크 경계가 다를 수 있어 필드를 분리하고 마스크를 박스로 잘라내지 않습니다.
  FiftyOne COCO 박스·픽셀 마스크 mAP/AP50, 클래스별 AP/AP50, IoU 0.5의 TP/FP/FN을 기록합니다.
  정답 객체가 없는 클래스의 AP는 `null`이며 전체 평균에서 제외합니다.
- `--view EVALUATE_JOB_ID`는 저장 결과를 열고, 평가 데이터셋이 없으면 아티팩트에서
  재구성합니다. GPU 추론은 반복하지 않습니다. 기본 표시 뷰와 박스/마스크 FP·FN,
  빈 예측 뷰를 제공합니다. 화면 서버는 이번 실행이 소유한 서버에 한해 종료 대기를 제한합니다.
- `comparison_id`는 실제 평가 이미지·정답·클래스 매핑·split·계산 설정에 결합합니다.
  학습 작업·학습 버전·저장 경로·표시 임계값을 제외해 같은 기준의 모델 비교를 허용합니다.
- 실패·중단 상태와 이미지별 오류를 남기며 일부 예측을 전체 평가로 취급하지 않습니다.
  현재 평가는 새 실행으로 다시 계산하고, 완료된 평가만 화면 복원을 허용합니다.
- MLflow를 먼저 불러온 뒤 ETA 0.17의 누락된 `author`/`home-page` 메타데이터 조회가
  실패하는 호환성 문제를 확인했습니다. `pipeline` extra에 `importlib-metadata==7.2.1`을
  고정하고 같은 프로세스의 실제 FiftyOne·MLflow 평가로 검증했습니다.

검증 범위:

- 단위 검증: 비정사각형 이미지, 분리된 마스크·구멍, 소수 좌표, 빈 마스크·빈 예측,
  잘못된 배열·클래스·점수 거부, 최대 검출 수, 선택 범위·비교 해시, CLI 호출과 초기화 실패 기록.
- 실제 FiftyOne·MLflow: 완벽한 예측은 박스/마스크 mAP 1, 박스만 맞으면 1/0,
  마스크만 맞으면 0/1, 빈 예측은 미탐, 빈 정답의 예측은 오탐으로 계산했습니다.
  부재 클래스 N/A, 평가 키·오류 뷰, 아티팩트만을 사용한 재구성과 변조 거부를 확인했습니다.
- 호스트 전체 회귀 검증: **129 passed, 2 skipped**. 별도 GPU·SAM 3 선택 테스트는 이
  집계에서 제외되며, 기존 검수·DVC·학습 기록·MLflow UI 테스트를 포함합니다.
- 실제 GPU 학습·새 프로세스 모델 복원 뒤 val/test 평가와 평가 데이터셋 삭제 후 복원은
  생성 이미지 4장, 희소 클래스 ID 7/42, 실제 DVC·MLflow·FiftyOne으로 검증했습니다.
  FiftyOne HTTP 200과 화면 서버 종료를 확인했습니다. 전체 학습·재개·평가·복원 약 92초,
  val/test 각 1장 평가 약 12초, 평가 프로세스 최대 RSS 약 2,242 MiB(2.19 GiB)였습니다.
  실행 기록은 `.vloop/evaluate-integration.json`이며 임시 데이터·모델·DB는 정리했습니다.
  실제 도메인 정확도나 대규모 처리의 메모리 상한을 측정한 값은 아닙니다.

대량 처리 제한: 추론과 입력 저장은 순차 처리하지만 FiftyOne의 COCO mAP 계산은 전체
객체 매칭을 메모리에 모읍니다. 따라서 100만 장 전체 평가의 메모리 상한을 보장하지 않습니다.
`--limit`으로 작은 범위부터 프로세스 최대 RSS를 측정해야 합니다. 이 제한은 DVC 복원 범위를
줄이지 않으며, MongoDB 프로세스 메모리와 평가 DB 저장 공간도 별도로 필요합니다.

## 7단계 공개 데이터 반복 실행 (2026-09-12)

사용자 요청에 따라 [Penn-Fudan 공식 데이터](https://www.cis.upenn.edu/~jshi/ped_html/)를
다운로드하고 사진 8장을 선택했습니다. `examples/pennfudan_toy.py`는 별도 소스 스냅샷 Git
저장소·설정·FiftyOne DB·DVC remote·MLflow에 결과를 남깁니다. 사용자의 기본 `project.yaml`과
메인 Git에는 데이터 버전이나 예시 클래스를 넣지 않았습니다.

- v001: train 4장·val 1장·test 1장. v002: train 2장을 추가해 6장, val/test는 그대로 유지.
- SAM 3는 첫 실행 6장, 다음 실행 8장을 모두 처리했습니다. 실패·빈 예측은 0건입니다.
- 예측을 보관하고, 제공된 객체별 정답 마스크를 검수용 GT에 반영했습니다. 승인 API를 실제
  사용하되 `toy-reference-import` 검수자와 메모로 스크립트 처리임을 기록했습니다.
  사람이 직접 검수한 데이터로 주장하지 않습니다. 두 번째 실행에서는 새 2장만 승인했습니다.
- 각 버전을 같은 사전학습 가중치에서 2 epochs 학습하고 둘 다 v001의 val로 평가했습니다.
  동일 `comparison_id`, val/test 데이터 해시와 v001 복원을 확인했습니다. test는 평가하지 않았습니다.
- v002 릴리스에서는 이미지 2장만 새로 저장하고 기존 6장을 재사용했습니다.
- 두 학습에서 PyTorch 최대 할당 VRAM은 약 1.262 / 1.275 GiB, 최대 예약 VRAM은
  1.295 / 1.326 GiB, 학습 프로세스 최대 RSS는 3,840.59 / 3,848.20 MiB였습니다.
  이 소형 입력의 관찰값이며 다른 프로세스·MongoDB 메모리나 대규모 입력의 상한은 아닙니다.

| 동일 v001 val의 지표 | v001 모델 | v002 모델 |
|---|---:|---:|
| 박스 mAP | 0.064922 | 0.137064 |
| 박스 AP50 | 0.072136 | 0.171711 |
| 마스크 mAP | 0.070934 | 0.087104 |
| 마스크 AP50 | 0.084158 | 0.100990 |

val 1장·2 epochs의 흐름 확인용 수치입니다. 실제 도메인 정확도나 통계적으로 유효한 개선
결과로 해석하지 않습니다. 실행 방법, 실제 작업 ID와 결과 위치는 [TOY.md](TOY.md)에 있습니다.

새 프로세스에서 `release` CLI만 실행하면 프로젝트 DB 설정 전에 FiftyOne을 import하는
문제도 발견해 수정했습니다. 기존 통합 테스트는 ingest와 release를 같은 프로세스에서 실행해
문제를 발견하지 못했습니다. 이제 릴리스는 상속된 DB 설정 경로 없이 별도 CLI로 검증합니다.

검증:

- 기본 회귀 검사 **124 passed, 10 skipped**. GPU·로컬 서비스 선택 검사는 기본 집계에서 제외.
- 실제 FiftyOne → 별도 release CLI → DVC → 복원 및 승인 후 수정 거부: **1 passed**.
- 별도 GPU 반복 runner 통과: 생성 이미지의 학습 라벨 1건 수정·이미지 추가, 고정 val 비교,
  v002의 기본 val 평가와 명시적 v001 val의 동등성, 이전 아티팩트 20개·태그·split·COCO ID 보존,
  v001 복원 후 현재 검수 정답 보존. 약 168초, 기록은 `.vloop/iteration-integration.json`.
- 공개 사진 toy는 실제 CLI로 위 두 루프를 완료했습니다. 사진·정답·모델·보고서·평가 DB는
  `.vloop/toy-pennfudan/`에 유지합니다. 회귀 runner의 임시 자료는 자동 정리합니다.
- 저장한 v002 평가를 `--view`로 열어 브라우저에서 보행자 이미지·정답 마스크와
  `all` 뷰의 예측 표시를 확인했습니다. 기본 표시 임계값 0.5에서는 이번 짧은 모델의 예측이
  모두 숨겨집니다. 낮은 임계값으로 저장·계산한 평가 결과는 그대로 유지됩니다.
- Ruff, 포맷, `pip check`, Git 공백 검사 통과.

## 전체 후보 표본 추출과 수동 평가 분할

사용자 요청에 따라 `release --manual-to-val-test`를 구현했습니다. 해당 릴리스에서 선택하면
신규 수동 승인 그룹은 val:test 비율로, 자동 채택 그룹은 train으로 배정합니다. 기본 0.1:0.1은
수동 승인분에 대해 1:1입니다. 같은 작업의 재개에는 저장한 정책을 사용하며, 기존 split·COCO ID와
누락된 이미지의 이력도 유지합니다. 이미 train에 들어간 이미지와 같은 그룹의 신규 이미지는
사람이 승인해도 train입니다. 기존 val/test 그룹의 새 이미지는 계속 보류합니다.

`review-batch --sample-rate`는 성공 예측이 있는 미검수 후보 전체에서 신뢰도 판정 전에
표본을 뽑습니다. 선택된 저신뢰도·빈 예측도 `sample`, 표본 외의 저신뢰도는 `low_confidence`,
빈 예측은 `empty`, 나머지는 자동 채택입니다. 기존 승인·편집 보존과 오류 처리 규칙은 유지합니다.
`release_group_field`가 있으면 표본도 그룹 단위로 선정합니다. 표본과 val/test 배분에 서로
다른 해시를 사용해 표본이 한 split에 몰리는 상관관계를 피합니다.

`--manual-to-val-test`를 사용하면 자동·수동 승인이 섞인 신규 그룹은 전체를 보류하며
`summary.held_reasons`와 SQLite의 `held_reason`으로 확인합니다. 전부 보류된 경우에도
준비 결과는 제공하고 빈 버전의 발행은 거부합니다. 같은 그룹의 자동 승인 멤버를 수동 검수한 뒤
새 미리보기에서 다시 확인할 수 있습니다.
검수의 `--job-id` 의미와 화면 필터는 사용자 요청에 따라 변경하지 않았습니다.

검증:

- 기본 회귀 검사 **129 passed, 11 skipped**. 선택 검사를 제외한 집계입니다.
- 실제 DVC 저장·캐시 삭제 후 복원·중단 후 새 프로세스 재개: 기본 정책과 새 정책 모두 통과.
  준비한 정책의 보존, 자동 train을 수동 재승인해도 train 유지, 이전 태그·코드 상태 보존을 확인했습니다.
- 실제 FiftyOne 표본 미리보기와 검수 재개, 별도 release CLI → DVC → 복원: **2 passed**.
  동일 그룹의 고신뢰도·저신뢰도·빈 예측이 함께 표본으로 뽑히는 것을 검증했습니다.
  샌드박스의 로컬 소켓 제한으로 실패한 DB 검사는 호스트 실행 권한을 받아 재검증했습니다.
- 신규 혼합 그룹 보류·검수 후 해소, 이후 버전의 신규 수동 그룹, 기존 평가 그룹 보류,
  버전에서 빠졌다가 돌아온 이미지의 ID·분할 보존과 그룹 변경 거부를 확인했습니다.
- README의 Penn-Fudan 수동 실습과 예제 YAML에 새 정책을 반영했습니다. 기존 실행 데이터와
  공개 정답을 사용한 과거 통합 검증 기록은 변경하지 않았습니다.

## 릴리스마다 분할 정책 선택

후속 사용자 요청에 따라 첫 버전의 정책을 강제로 계승하던 제약을 제거했습니다.
`--manual-to-val-test`와 `--include-auto-accepted`는 새 릴리스마다 독립적으로 선택합니다.
옵션을 생략하면 후속 버전에서도 신규 수동 그룹을 train/val/test 비율로 배정합니다.
옵션을 지정하면 신규 수동 그룹은 val/test 비율만 사용합니다. 자동 채택은 명시적으로 포함한
경우 train에만 들어가며, 기존 이미지·그룹 분할과 COCO ID는 정책을 바꿔도 유지합니다.

새 릴리스의 `seed`, `split_ratios`는 새 그룹에만 적용합니다. 같은 작업을 `--resume`으로
재개하면 당시 설정·정책·스냅샷을 그대로 사용합니다. 기본 모드의 신규 혼합 그룹은 train,
수동 평가 모드의 신규 혼합 그룹은 보류하며 기존 평가 그룹의 신규 이미지는 계속 보류합니다.
README에 네 옵션 조합과 v002에서의 선택 방법을 반영했습니다.

검증: 기본 회귀 검사 **133 passed, 11 skipped**, 실제 DVC 저장·복원·새 프로세스 재개
**2 passed**. 네 옵션 조합, 버전 사이의 정책 켜기/끄기, 기존 split·COCO ID 보존, 자동 라벨이
평가에 들어가지 않는 그룹 배정, 재개 시 변경된 YAML 대신 저장한 설정 사용을 확인했습니다.
CLI 네 조합과 Penn-Fudan 셸 블록 14개·예제 YAML, Ruff·포맷·Git 공백 검사도 통과했습니다.

## 의존성 버전 고정

검증한 Linux x86_64·CPython 3.12·CUDA 12.8 환경을
`requirements/linux-py312-cu128.lock`에 기록했습니다. 전체 extra와 SAM 3 이미지 추론에 필요한
외부 패키지 285개를 전이 의존성까지 고정하며, PyTorch CUDA 인덱스도 지정합니다.
`requirements/bootstrap.lock`으로 pip·setuptools·wheel을 먼저 고정하고 소스 빌드의 격리 환경을
사용하지 않습니다. 두 파일에서 겹치는 setuptools를 제외하면 외부 패키지는 총 287개입니다.
프로젝트와 SAM 3는 별도 editable 설치를 유지하고 SAM 3는 기존 검증 커밋을 사용합니다.

`requirements/check.py`는 설치 버전과 현재 프로젝트 의존성을 대조하고 중첩 extra까지 따라가
누락·충돌을 검사합니다. README의 새 clone 설치 절차에 lock을 반영했으며,
[의존성 고정 안내](../requirements/README.md)에 전체·부분 설치와 갱신 방법을 정리했습니다.
이번 범위는 패키지 버전 고정이며, 배포 파일 해시·OS·드라이버·체크포인트 고정은 포함하지 않습니다.

검증:

- 같은 호스트의 별도 빈 가상환경 `.vloop/lock-validation-venv`에 bootstrap, 전체 lock,
  프로젝트와 SAM 3를 순서대로 설치했습니다. Ubuntu 24.04.5·CPython 3.12.3 기준입니다.
  기존 `.venv`, 사용자 설정·데이터·모델은 유지했습니다.
- `pip check`와 lock 검사 통과. 중첩 extra 누락·버전 충돌·잘못된 고정 항목의 회귀 검사를
  포함한 기본 검사 **136 passed, 11 skipped**.
- 실제 DVC 저장·복원·새 프로세스 재개와 RF-DETR 데이터 로더 검사 **2 passed**.
- 실제 FiftyOne DB의 이미지·정답 보존과 MLflow 서버의 실행 기록·아티팩트 조회 검사
  **2 passed**. 샌드박스의 로컬 소켓 제한 이후 호스트 권한으로 재검증했습니다.
- SAM 3 소스 커밋·editable 설치 위치 확인, SAM 3·RF-DETR import와 RTX 5060 Laptop의
  CUDA 12.8 행렬 연산 통과. 샌드박스의 GPU 접근 제한 이후 호스트 권한으로 재검증했습니다.
- Ruff·포맷·Git 공백 검사와 설치 셸 블록 문법 검사 통과. 새 환경에서 전체 GPU 학습이나
  브라우저의 수동 마스크 편집을 다시 수행한 검증은 아닙니다.
- 설치 보고서는 `.vloop/dependency-lock-install.json`, `.vloop/dependency-lock-sources.json`에
  기록했습니다. 검증 가상환경과 다운로드 캐시는 `.vloop/` 안에 두어 Git에서 제외합니다.

## 최종 코드·테스트·문서 정리 (2026-09-12)

- `device: cuda`를 SAM 3 초기화 전에 `cuda:0`으로 변환합니다. 명시적인 GPU 번호가 없는
  `torch.device`를 `set_device()`에 넘기면 실패하던 문제를 수정했습니다.
- 자동 라벨링의 상태 집계는 실행 시작에 한 번 수행하고 이미지 처리 후에는 해당 행의
  변화만 반영합니다. 매 이미지마다 전체 작업 목록을 다시 집계하던 비용을 제거했습니다.
- 클래스 ID의 모델 인덱스를 오름차순으로 통일하고 COCO 릴리스에서 같은 매핑을 사용합니다.
  YAML의 클래스 순서를 바꿔도 RF-DETR의 매핑과 일치합니다.
- 사용하지 않는 이전 검수 감사 함수와 MLflow 생성 함수의 미사용 인자를 제거했습니다.
  검수 통합 테스트는 실제 `review-audit` 경로를 사용하며, 감사 초기화 실패·첫 배치 전 중단과
  재개 기록을 보완했습니다.
- 재실행·미리보기 적용·평가 화면 안내 명령에 설정 경로를 보존하고 셸 인용을 적용했습니다.
  공백이나 특수문자가 있는 경로도 같은 프로젝트를 가리킵니다.
- 릴리스 규모 측정 스크립트의 SQLite 구조를 현재 내보내기 코드에 맞췄습니다.
  세 규모 측정 스크립트를 작은 입력으로 실제 실행하는 회귀 검사를 추가했습니다.
- 전체 테스트에서 FiftyOne이 앞 테스트의 MongoDB를 재사용하는 문제를 발견했습니다.
  실제 연결의 DB 경로를 검사해 다른 프로젝트의 데이터 접근을 거부하고, DB 통합 테스트를
  별도 프로세스로 격리했습니다. 같은 사용자의 서로 다른 프로젝트 동시 실행 제한도 명시했습니다.
- README의 옵션별 분할 비율, 검수 queue와 작업 ID, 평가 비교·화면 재열기, 의존성 고정의
  범위를 명확히 했습니다. 초기 계획 본문과 과거 toy 결과는 현재 사용 안내와 구분했습니다.

최종 검증:

- 전체 lock 환경에서 모든 선택 검사를 활성화해 **159 passed, 0 failed, 0 skipped**를
  확인했습니다. 소요 시간은 약 6분 24초입니다. 실제 SAM 3 GPU 추론·재개, RF-DETR 학습·재개·
  두 버전 비교, FiftyOne 검수·평가, DVC 저장·복원, MLflow HTTP·아티팩트 검사를 포함합니다.
- 실제 DB 포트를 상속하는 경우와 서비스 자동 검색으로 연결하는 경우 모두 다른 프로젝트의
  DB 접근을 거부했습니다. 같은 프로젝트의 별도 프로세스 재등록과 라벨 보존은 통과했습니다.
- 외부 패키지 287개·로컬 패키지 2개의 lock 검사, `pip check`, Ruff·포맷·Git 공백 검사 통과.
  외부 라이브러리의 deprecation·학습 설정 경고는 테스트 출력에 남아 있습니다.
- 셸 블록 55개, 현재 안내의 CLI 예제 59개, YAML 블록 4개와 로컬 문서 링크 19개를 확인했습니다.
  wheel을 빌드하고 현재 프로젝트 소스·FiftyOne 플러그인·CLI 진입점이 포함되는지도 확인했습니다.
- 전체 테스트 로그·JUnit 결과는 `.vloop/final-review-pytest.log`와
  `.vloop/final-review-pytest.xml`에 있습니다. 사용자 설정·기존 데이터·모델·릴리스는 유지했으며,
  이번에는 브라우저에서 사람이 마스크를 편집하는 검증을 반복하지 않았습니다.

이번 수정으로 SAM 3 adapter와 검수 판정 코드의 해시가 바뀌었습니다. 기존의 미완료
`autolabel` 작업과 `review-batch` 미리보기는 원래의 코드 일치 정책에 따라 새 작업으로 시작합니다.
이미 저장한 예측·승인·릴리스는 삭제하지 않습니다.

## 다음 확인 지점

사용자 요청에 따라 `project.example.yaml`에 Penn-Fudan의 사진 경로
`data/PennFudanPed/PNGImages`와 `person` 클래스를 채웠습니다. Python 설정 기본값은 여전히
빈 입력·빈 클래스이며, 예제 YAML을 명시적으로 복사해야 적용됩니다. 원본 PLAN의 빈 예제 방침은
보관하고 현재 사용자 요청을 반영했습니다. 기본 `project.yaml`과 기존 실행 데이터는 수정하지 않았습니다.

[README의 수동 실습](../README.md#penn-fudan-직접-실습)은 새 clone의 설치·SAM 3 준비부터
`curl` 다운로드·사진만 압축 해제·자동 라벨링·`sample-rate 0.2` 채택 미리보기와 적용·사람의
표본 검수·릴리스·학습·평가·v002 비교까지 안내합니다. 공개 정답 가져오기는 사용하지 않습니다.
기존 [8장 자동 통합 검증 기록](TOY.md)은 공개 정답을 사용한 별도 기록으로 유지합니다.

수동 실습 안내 검증: 임시 새 폴더에 예제 YAML을 복사하고 기존 공식 ZIP에 README의 `unzip`
선택 추출을 적용해 사진 170장·정답 마스크/주석 미추출·입력 설정 유효성을 확인했습니다.
실습의 셸 블록 14개 문법 검사와 설정 테스트 27개, Ruff·포맷·Git 공백 검사가 통과했습니다.
이번 문서 검증에서 사용자 대신 검수·승인하거나 새 학습을 실행하지는 않았습니다.

현재 수동 실습은 첫 릴리스의 `--manual-to-val-test`로 표본 검수분을 val/test에 집중합니다.
표본 20%를 모두 수동 승인하고 나머지 80%가 자동 채택되면 전체는 약 80/10/10이 됩니다.
실제 장수는 표본·승인 상태·그룹 크기에 따라 달라집니다. 옵션 없는 기본 분할도 유지합니다.

실제 이미지 폴더와 클래스별 ID·이름·프롬프트를 설정한 뒤 `ingest`, `autolabel --limit 3`으로
도메인 데이터의 출력과 메모리를 확인하고 `review`로 검수합니다. 4단계는
`release --prepare-only`로 대상과 예상 공간을 확인한 다음 출력된 작업 ID로 재개할 수 있습니다.
자동 채택은 `--include-auto-accepted`일 때만 train에 포함합니다. 학습 코드를 커밋한 뒤
`vloop train --dataset-version v001`로 실제 도메인 데이터의 첫 기준 모델을 학습할 수 있습니다.

7단계의 두 버전 실행·비교 경로는 공개 toy 데이터로 확인했습니다. 운영 데이터 적용 시에는
충분한 독립 val에서 오류를 분석하고 검수·추가 데이터를 반영해 비교해야 합니다.
의존성 버전 고정과 같은 호스트의 빈 가상환경 설치 검증은 완료했습니다.
실제 도메인 데이터의 개선 검증, 대규모 실측과 완전히 다른 컴퓨터에서의
설치·GPU·검수 화면을 포함한 재검증은 남아 있습니다.
